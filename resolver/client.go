package resolver

func InsertPassword(website, username, password string) error {
	// find the user
	// user, err := db.GetUsers(username)
	// if err != nil {
	// 	return err
	// }

	// if len(user) == 0 {
	// 	return errors.New("found no users with that username. Please sign up")
	// }

	return nil
}
